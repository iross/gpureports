[project]
name = "single_file_submit"
version = "0.1.0"
description = "Experiments in single-file interfaces for HTCondor joblist submission"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.8",
    "elasticsearch>=8.17.1",
    "htcondor>=24.3.0",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.0",
    "pandas>=2.2.3",
    "ruff>=0.8.6",
]

[tool.uv.workspace]
members = ["place_joblist"]
